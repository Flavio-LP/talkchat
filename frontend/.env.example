# Copy to .env.local if your backend is not on the default address.
# Anything prefixed with VITE_ is bundled into the browser build, so never put
# secrets here — the Gemini key lives only in backend/.env.
VITE_API_BASE_URL=http://localhost:3000/api/v1
